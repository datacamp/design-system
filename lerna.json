{
  "packages": ["packages/**/*"],
  "version": "independent",
  "ignoreChanges": ["*.md", "*.stories.js", "*.spec.*"],
  "npmClient": "yarn",
  "command": {
    "version": {
      "allowBranch": "master"
    },
    "publish": {
      "npmClient": "npm"
    }
  }
}
